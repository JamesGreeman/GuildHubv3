{
  "name": "guild-hub",
  "version": "0.0.1",
  "description": "Dependencies for new project",
  "main": "index.js",
  "dependencies": {
    "react": "*"
  },
  "devDependencies": {
    "browserify": "*",
    "envify": "*",
    "reactify": "*",
    "statics": "*",
    "uglifyjs": "*",
    "watchify": "*"
  },
  "scripts": {
    "start": "STATIC_ROOT=./static watchify -o browser-bundle.js -v -d .",
    "build": "STATIC_ROOT=./static NODE_ENV=production browserify . | uglifyjs -cm > browser-bundle.min.js",
    "collect-static": "collect-static . ./static",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "James Greeman",
  "license": "Apache 2",
  "browserify": {
    "transform": [
      "reactify",
      "envify"
    ]
  }
}
